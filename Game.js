import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function Game() {
    const screen = setState('splash')

    return (
        <View>
            <Text>Game</Text>
        </View>
    );
}

